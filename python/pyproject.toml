[build-system]
requires = [
    "setuptools>=69",
    "wheel",
    "pybind11>=2.12",
    "numpy>=1.23"
]
build-backend = "setuptools.build_meta"

[project]
name = "hydrogenic-demo"
version = "0.1.0"
description = "Python bindings for the Hydrogenic-demo C++ atom routines"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.23"
]
authors = [
    { name = "Hydrogenic-demo contributors" }
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: C++",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Physics"
]

[project.optional-dependencies]
notebooks = [
    "jupyterlab>=4.0",
    "notebook>=7.0",
    "matplotlib>=3.7"
]

[tool.setuptools.packages.find]
where = ["src"]
